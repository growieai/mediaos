# Mode 0600, outside Git. Mounted only by explicitly run maintenance containers.
# This password must match the separate postgres_admin_password file.
MIGRATION_DATABASE_URL=postgresql+psycopg://mediaos_admin:REPLACE@postgres:5432/mediaos
# Must match the decoded password from runtime.env DATABASE_URL.
POSTGRES_RUNTIME_PASSWORD=REPLACE
